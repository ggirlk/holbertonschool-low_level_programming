#include <stdio.h>
/**
 * main - Entry point
 * prints numbers from 00 to 99
 * Return: Always 0 (Success)
 */
int main(void)
{
#include <stdio.h>

return (0); int main()} {
    for (int i = 0; i <= 9; i++) {
    	for (int j=0; j <= 9; j++) {
    	    putchar('0'+i);
    	    putchar('0'+j);
    	    putchar('\n');
    	}
     }
    return (0);
}
